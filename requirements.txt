fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pymupdf>=1.24.0
transformers>=4.40.0
torch>=2.2.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
chromadb>=0.5.0
openai>=1.0.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
# pinned to 4.0.1: bcrypt 4.1.x introduced a passlib incompatibility;
# this project uses bcrypt directly (not via passlib) so the pin can be
# lifted once upstream confirms stability — verify before upgrading
bcrypt==4.0.1
email-validator>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
slowapi>=0.1.9
# Used only by scripts/archive_period.py to write the per-period archive
# report.xlsx. Not imported by the FastAPI app at runtime.
openpyxl>=3.1.0
